<%- include("partials/header"); -%>

    <div class="quesContainer">
        <%  for(let i = 0; i < resp.length && i < 10; i++){ %>

            <div class="question">
                <div class="title"><h3><%=resp[i].title%></h3></div>
                <div class="body">
                    <p>
                        <%=resp[i].statement.substr(0, 50) + " ..."%>
                        <a href="">Read More</a>
                    </p>
                </div>
            </div>

        <% } %>

    </div>

</body>
</html>